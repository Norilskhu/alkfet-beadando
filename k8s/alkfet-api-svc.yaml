apiVersion: apps/v1
kind: Deployment
metadata:
  name: alkfet-api-svc
  namespace: alkfet
  labels:
    app: alkfet-api-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: alkfet-api-svc
  template:
    metadata:
      labels:
        app: alkfet-api-svc
    spec:
      containers:
        - name: alkfet-api-svc
          image: ghcr.io/norilskhu/alkfet-beadando/alkfet-api-svc:latest
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_PORT
              value: "8080"
            - name: APP_DB-SVC_BASE-URL
              value: "http://alkfet-db-svc:8081"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: alkfet-api-svc
  namespace: alkfet
spec:
  selector:
    app: alkfet-api-svc
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

